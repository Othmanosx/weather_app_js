import React from 'react'
import './info.css'

export default function more(props) {
    return (
        <div>
            {/* <ul>{props.state.date.map((date) => (<td>{date}</td>))}</ul>

            <table>
                <thead>
                    <tr>
                        <th>weather</th>
                        <th>date</th>
                        <th>low</th>
                        <th>high</th>
                        <th>average</th>
                        <th>humidity</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        {props.state.date.map((date) => (<td>{date}</td>))}
                        {props.state.date.map((date) => (<td>{date}</td>))}
                        {props.state.date.map((date) => (<td>{date}</td>))}
                        {props.state.date.map((date) => (<td>{date}</td>))}
                        {props.state.date.map((date) => (<td>{date}</td>))}
                        {props.state.date.map((date) => (<td>{date}</td>))}
                    </tr>

                </tbody>
            </table> */}

        </div>
    )
}